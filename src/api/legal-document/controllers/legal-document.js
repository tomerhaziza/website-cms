'use strict';

/**
 * legal-document controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::legal-document.legal-document');
